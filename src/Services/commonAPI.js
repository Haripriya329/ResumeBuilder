import axios from "axios"
import { BiErrorAlt } from "react-icons/bi"

const commonAPI = (httpMethod,url,reqBody) =>{
    axios({
        method:httpMethod,
        url,
        data:reqBody
    }
return await axios(reqConfig).them(res=>{
return res
}).catch()git =>{
    return err
})
}